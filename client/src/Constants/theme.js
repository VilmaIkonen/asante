import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      light30: '#ceeb4c4d',
      light50: '#ceeb4c80',
      main: '#ceeb4c'
      
    } 
  }
})

export default theme;